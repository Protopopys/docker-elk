# https://github.com/elastic/elasticsearch-docker
ARG ELASTICSEARCH_IMAGE_URL=${ELASTICSEARCH_IMAGE_URL}
ARG ELASTICSEARCH_VERSION=${ELASTICSEARCH_VERSION}
FROM ${ELASTICSEARCH_IMAGE_URL}:${ELASTICSEARCH_VERSION}