version: "3.6"

services:

  elasticsearch:
    environment:
      - discovery.type=single-node
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - ${ELASTICSEARCH_DATA:-/elk/elasticsearch}:/usr/share/elasticsearch/data:rw
      - $PWD/images/elasticsearch/config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
    networks:
      elk:
        aliases:
          - elasticsearch
    restart: on-failure

  logstash:
    volumes:
      - $PWD/images/logstash/config/logstash.yml:/usr/share/logstash/config/logstash.yml:ro
      - $PWD/images/logstash/pipeline/logstash.conf:/usr/share/logstash/pipeline/logstash.conf:ro
    networks:
      elk:
        aliases:
          - logstash
    depends_on:
      - elasticsearch
    restart: on-failure

  kibana:
    ports:
      - "5601:5601"
    volumes:
      - ${KIBANA_DATA:-/elk/kibana}:/usr/share/kibana/data:rw
      - $PWD/images/kibana/config/kibina.yml:/usr/share/kibana/config/logstash.yml:ro
    networks:
      elk:
        aliases:
          - kibana
    depends_on:
      - elasticsearch
    restart: on-failure

networks:
  elk:
    driver: bridge