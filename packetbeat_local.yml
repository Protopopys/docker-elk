version: "3.6"

services:

  packetbeat:
    volumes:
      - ${PACKETBEAT_DATA:-/elk/packetbeat}:/usr/share/packetbeat/data:rw
      - $PWD/beats/packetbeat/config/packetbeat.yml:/usr/share/packetbeat/packetbeat.yml:ro
    environment:
      - ELASTICSEARCH_HOST=${ELASTICSEARCH_HOST}
      - KIBANA_HOST=${KIBANA_HOST}